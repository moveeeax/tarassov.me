apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cpp-api.fullname" . }}-test-connection"
  labels:
    {{- include "cpp-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cpp-api.fullname" . }}:{{ .Values.service.httpPort }}/healthz', '-O', '-', '-q']
  restartPolicy: Never
