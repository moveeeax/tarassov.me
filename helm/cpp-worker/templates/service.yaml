apiVersion: v1
kind: Service
metadata:
  name: {{ include "cpp-worker.fullname" . }}
  labels:
    {{- include "cpp-worker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.healthPort }}
      targetPort: health
      protocol: TCP
      name: health
    - port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "cpp-worker.selectorLabels" . | nindent 4 }}
