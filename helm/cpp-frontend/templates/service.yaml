apiVersion: v1
kind: Service
metadata:
  name: {{ include "cpp-frontend.fullname" . }}
  labels:
    {{- include "cpp-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cpp-frontend.selectorLabels" . | nindent 4 }}
